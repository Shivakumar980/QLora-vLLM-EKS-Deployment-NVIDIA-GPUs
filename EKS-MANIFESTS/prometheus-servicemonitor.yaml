apiVersion: v1
kind: ServiceMonitor
metadata:
  name: vllm-metrics
  namespace: vllm
spec:
  selector:
    matchLabels:
      app: vllm-llama
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
